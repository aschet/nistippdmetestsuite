OnScanReport.txt

The OnScanReport.prg file calls OnScanReport with various combinations
of arguments, all of which are legal. In particular, the arguments do not
need to be in any fixed order.

The OnScanReport.res file contains the responses to the commands
in the OnScanReport.prg file.
